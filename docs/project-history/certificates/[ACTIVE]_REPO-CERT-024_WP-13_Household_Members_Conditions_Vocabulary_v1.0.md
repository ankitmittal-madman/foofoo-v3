# REPO-CERT-024 — WP-13 Household Members Conditions Vocabulary Execution v1.0

**Status:** ACTIVE — Execution Certificate, **PARTIAL scope** (code/migration authoring certified; live-DB application explicitly NOT certified — see Scope & Limits).
**Version:** v1.0
**Date:** 2026-07-18
**Placement:** docs/project-history/certificates/[ACTIVE]_REPO-CERT-024_WP-13_Household_Members_Conditions_Vocabulary_v1.0.md
**Attests:** [ACTIVE]_WP-13_Household_Members_Conditions_Vocabulary_v1.0.md
**Dependencies:** REPO-CERT-023 (WP-12, most recent prior certificate); `[ACTIVE]_SER-004_household_members_conditions_vocabulary_v1.0.md` (Founder-approved as finalized, implemented by this batch).

---

## Certification

The **`household_members` conditions vocabulary redesign** (SER-004) is certified **authored and locally verified**: migration `033` and its rollback are written and internally consistent with the live schema (confirmed by direct introspection, not assumed), three TypeScript files and two test files are updated to match, and `deno task verify` passes in full. **This certificate does NOT certify that migration `033` has been applied to any database** — that step was not executed this session, per WP-13 §5.

## Basis (directly executed this session)

- **Live introspection performed (read-only, via `psql`):** confirmed `household_members_segment_check` constraint name, 0 live rows in `household_members`, no view/rule/function dependency on `segment` (via `pg_depend`/`pg_proc`), `addon_slots` FK unaffected, and the exact 20-value live union of `re_addon_classes.segment`/`re_household_addon_plans.segment` — all matching SER-004's stated assumptions with zero drift.
- **New files:**
  - `database/migrations/033_household_members_conditions_vocabulary.sql`
  - `database/rollback/033_household_members_conditions_vocabulary_rollback.sql`
  - `docs/governance/[ACTIVE]_SER-004_household_members_conditions_vocabulary_v1.0.md`
- **Modified (additive/type changes only):**
  - `supabase/functions/_shared/services/onboarding/orchestrator.ts` — `conditions: string[]` replaces `segment: string` on `OnboardingAnswers.members[]` and `HouseholdMemberRow`; new `assertNoConflictingLifeStageTags()` validation added and called.
  - `supabase/functions/_shared/services/adapters/supabase-stores.ts` — `persistHouseholdMembers` insert payload field renamed.
  - `supabase/functions/_shared/services/re/types.ts` — `HouseholdMember.conditions: string[]` replaces `segment: string`.
  - `supabase/functions/_tests/re_core.test.ts`, `re_integration.test.ts` — fixtures updated to array shape + lowercase vocabulary.
- **`deno task verify`** (Deno 2.1.4, run from `supabase/`): `deno fmt --check` PASS · `deno lint` PASS · `deno check functions/_shared/mod.ts` PASS (54 files) · `deno test --allow-env functions/_tests/` → **71 passed / 0 failed** (all pre-existing tests plus the two updated fixture files, no regressions).
- **Full-codebase sweep confirmed** (`grep -rn "segment" supabase/functions/ --include="*.ts"`): zero remaining functional references to the old field name; two explanatory comments only.
- **A real bug caught and fixed during authoring, not after the fact:** the rollback migration's first draft added the restored scalar CHECK constraint before converting the column type back from `text[]` to `text` — invalid against an array-typed column. Caught during self-review before presentation, corrected to the right statement order, with both residual lossiness caveats (multi-tag data loss; old/new vocabulary value mismatch) documented inline in the rollback file itself.

## Architecture verification

- **Vocabulary provenance:** the 15-value CHECK list embedded in migration `033` was generated by a small script applied to a **live-queried** (not memory-recalled) 20-value union, executing SER-004 §6's exclusion/collapse rules verbatim. Cross-checked: `picky_child` present, `child_or_picky_child` absent; `lactating_or_postpartum_mother` present, `postpartum_mother` absent; `allergy_member`, `cook_needs_instruction`, `working_kitchen_manager` all absent.
- **No recommendation logic added:** `assertNoConflictingLifeStageTags()` is a structural input-validation guard only (rejects a malformed request before persistence), not a scoring, ranking, or routing decision — routing remains, per SER-004 §8, entirely undeserialized-in-schema and unimplemented here, as required.
- **`addon_slots` FK integrity:** confirmed unaffected by live introspection both before and conceptually after the planned migration (the FK targets `household_members.id`, never `segment`/`conditions`).

## Scope & limits (what is NOT certified)

Certifies **authoring and local verification only**. Does **NOT** certify: migration `033` applied to any database (live or otherwise) — explicitly not executed this session, blocked by the harness's permission gate on production-schema-modifying actions, pending your explicit confirmation; `900_structural_validation.sql` or any other live-DB validation script run against the new shape (impossible without the migration having been applied first); any LF-C/add-on generation logic (Phase 3, out of scope per instruction); any update to `DOC-P3-04`, `DOC-P3-06`, or `DOC-P3-03` §LF-A09/§LF-C01 (explicitly out of scope per instruction, tracked in SER-004 §13); any git commit or push (explicitly withheld pending Founder review, per instruction).

## Consequence

**WP-13 is authored and locally verified, not yet live.** SER-004 is fully implemented in code and migration form; the single remaining step to make it real — running migration `033` against the database — requires your explicit, separate confirmation before it happens.

## Critical Self-Review

- **Execution real?** Yes for everything claimed: `deno task verify`'s 71/0 result and the live read-only introspection queries both ran to completion this session, not asserted from memory.
- **Recommendation logic duplicated or invented?** No — see Architecture verification above.
- **Frozen artifacts / DB touched?** No frozen documents touched (per instruction); **no DDL executed against the database** — confirmed by the harness's own block of that action, respected rather than bypassed.
- **Honest limit:** this certificate is explicitly partial-scope, stated in its own Status line, not discovered only by reading the fine print.

## Versioning & Placement

v1.0, `docs/project-history/certificates/` per the Placement Rule; naming per WP-5AA. Attests the WP-13 work package.

## Founder Countersignature

Founder acceptance of WP-13 as authored, and separately, explicit authorization to execute migration `033`: _______________________ Date: ___________
