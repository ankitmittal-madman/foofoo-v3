"""Service version identity. engine_version is the RE service's own version (image tag per
RE-DOC-10 §13); config_version comes from the loaded EngineConfig; api_version is the URL contract."""

API_VERSION = "v1"
ENGINE_VERSION = "1.0.0"
